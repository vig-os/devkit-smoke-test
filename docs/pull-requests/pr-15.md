---
type: pull_request
state: closed (merged)
branch: dev → main
created: 2026-03-07T09:16:42Z
updated: 2026-03-09T14:05:50Z
author: c-vigo
author_url: https://github.com/c-vigo
url: https://github.com/vig-os/devkit-smoke-test/pull/15
comments: 16
labels: none
assignees: none
milestone: none
projects: none
merged: 2026-03-09T14:05:50Z
synced: 2026-07-10T15:45:45.990Z
---

# [PR 15](https://github.com/vig-os/devkit-smoke-test/pull/15) chore: merge dev into main for dispatch smoke-test validation

## Description

Promote `dev` to `main` to enable post-merge validation of the smoke-test dispatch orchestration wired in PR #14.

`repository_dispatch` workflows execute from the repository's default branch definition. Until these changes land on `main`, the validate/orchestrate/summary flow in `repository-dispatch.yml` cannot be tested via the dispatch API.

## Type of Change

- [x] `chore` -- Maintenance task (deps, config, etc.)
- [x] `ci` -- CI/CD pipeline changes

## Changes Made

Accumulated `dev` changes since last promotion:

1. **CI: wire smoke-test dispatch to run RC CI variants** (PR #14, issue #13)
   - `repository-dispatch.yml`: replaced stub with validate/orchestrate/summary flow
   - `ci-container.yml`: added `workflow_call` input `image-tag` (default `latest`)
   - `ci.yml`: added `workflow_call` trigger

2. **Dependabot dependency bumps**
   - `actions/cache` 4.3.0 → 5.0.3 (#10)
   - `actions/checkout` 4.3.1 → 6.0.2 (#9)
   - `actions/upload-artifact` 4.6.2 → 7.0.0 (#8)
   - `actions-minor-patch` group update (#7)

3. **Devcontainer updates** (#11)
   - Updated devcontainer image and template
   - New scripts: agent identity check, branch summary, commit-msg trailers, skill name check
   - Updated justfiles and worktree tooling

4. **Documentation**
   - Added issue and PR docs (issues #1, #3, #6; PRs #2, #4, #5, #7–#10)
   - Changelog entries for Dependabot updates

## Changelog Entry

No new changelog entry needed — all entries were already added in the individual PRs on `dev`.

## Testing

### Pre-merge
- CI workflows run on this PR via existing `push`/`pull_request` triggers

### Post-merge dispatch validation (primary goal)

1. **Success path:**
```bash
gh api repos/vig-os/devcontainer-smoke-test/dispatches \
  -X POST \
  -f event_type='smoke-test-trigger' \
  -f client_payload[event_type]='rc-published' \
  -f client_payload[rc_tag]='latest' \
  -f client_payload[source_repo]='manual-test'
```
Expected: `validate`, `ci-bare`, `ci-container`, and `summary` all succeed.

2. **Missing `rc_tag`:**
```bash
gh api repos/vig-os/devcontainer-smoke-test/dispatches \
  -X POST \
  -f event_type='smoke-test-trigger' \
  -f client_payload[event_type]='rc-published' \
  -f client_payload[source_repo]='manual-test'
```
Expected: `validate` fails, `summary` fails.

3. **Invalid image tag:**
```bash
gh api repos/vig-os/devcontainer-smoke-test/dispatches \
  -X POST \
  -f event_type='smoke-test-trigger' \
  -f client_payload[event_type]='rc-published' \
  -f client_payload[rc_tag]='does-not-exist-tag' \
  -f client_payload[source_repo]='manual-test'
```
Expected: `validate` succeeds, `ci-container` fails image pull, `summary` fails.

## Checklist

- [x] My code follows the project's style guidelines
- [x] I have performed a self-review of my code
- [x] I have updated `CHANGELOG.md` in the `[Unreleased]` section (and pasted the entry above)
- [x] My changes generate no new warnings or errors
- [ ] Any dependent changes have been merged and published

## Additional Notes

- This is a standard `dev → main` promotion. All changes were individually reviewed and merged via their own PRs.
- The primary motivation is unblocking dispatch API testing for issue #13.

Refs: #13, #14


---
---

## Review Threads (6)

### Review by [@Copilot](https://github.com/apps/copilot-pull-request-reviewer)

_Posted on March 7, 2026 at 09:28 AM_

_File: [`.githooks/prepare-commit-msg (line 11 RIGHT)`](https://github.com/vig-os/devkit-smoke-test/pull/15#discussion_r2899397123)_

```diff
@@ -0,0 +1,14 @@
+#!/bin/bash
+# Strip AI agent trailers from commit message before validation.
+# Refs: #163
+set -euo pipefail
+
+# Check if we're running inside the dev container
+if [ "${IN_CONTAINER:-}" = "false" ]; then
+	# Outside dev container
+	echo "Please commit your changes within the dev container."
+	exit 1
+fi
```

The container check is inverted: outside the devcontainer, `IN_CONTAINER` is typically unset (not the literal string `false`), so this hook won’t block commits on the host. If the intent is to require commits to happen inside the devcontainer, check for `IN_CONTAINER != "true"` (or fail closed when the variable is unset).

Conversation:

- **[@c-vigo](https://github.com/c-vigo)** on March 9, 2026 at 06:58 AM — [link](https://github.com/vig-os/devkit-smoke-test/pull/15#discussion_r2903550357)

  Valid — filed upstream as [vig-os/devcontainer#238](https://github.com/vig-os/devcontainer/issues/238) (Bug C).

- **[@c-vigo](https://github.com/c-vigo)** on March 9, 2026 at 10:51 AM — [link](https://github.com/vig-os/devkit-smoke-test/pull/15#discussion_r2904654717)

  Implemented in #21

---

### Review by [@Copilot](https://github.com/apps/copilot-pull-request-reviewer)

_Posted on March 7, 2026 at 09:28 AM_

_File: [`CHANGELOG.md (line 29 RIGHT)`](https://github.com/vig-os/devkit-smoke-test/pull/15#discussion_r2899397133)_

```diff
@@ -17,9 +17,23 @@ and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0
 - **repository_dispatch listener stub** ([#3](https://github.com/vig-os/devcontainer-smoke-test/issues/3))
   - Added a dedicated workflow that listens for `repository_dispatch` events
   - Logs dispatch payload metadata as a minimal foundation for later cross-repo integration
+- **Wire smoke-test dispatch to run RC CI variants** ([#13](https://github.com/vig-os/devcontainer-smoke-test/issues/13))
+  - Upgraded `repository-dispatch.yml` from stub to validate/orchestrate/summary flow
+  - Parameterized `ci-container.yml` image tag via `workflow_call` input (default `latest`)
+  - Added `workflow_call` trigger to `ci.yml` for reusable invocation
 
 ### Changed
 
+- **Merged Dependabot GitHub Actions branches** ([#6](https://github.com/vig-os/devcontainer-smoke-test/issues/6))
+  - Merged `actions/cache-5.0.3`, `actions/checkout-6.0.2`, and `actions/upload-artifact-7.0.0`
+  - Included `actions-minor-patch-a1735c214c` to pick up additional workflow pin updates
```

This changelog entry claims Dependabot GitHub Actions branches were merged (cache v5, checkout v6, upload-artifact v7), but the workflows in this branch still pin `actions/checkout` and `actions/upload-artifact` to v4 SHAs, and `actions/cache` is still v4 in `sync-issues.yml`. Either update the workflow pins to match the stated upgrades, or adjust/remove this changelog entry so it reflects what actually shipped.
```suggestion
- **Reconciled Dependabot GitHub Actions branches** ([#6](https://github.com/vig-os/devcontainer-smoke-test/issues/6))
  - Reviewed and merged Dependabot branches for GitHub Actions workflows to align with the current CI setup
  - Captured minor workflow pin and configuration cleanups without changing the currently effective action versions
```

Conversation:

- **[@c-vigo](https://github.com/c-vigo)** on March 9, 2026 at 06:58 AM — [link](https://github.com/vig-os/devkit-smoke-test/pull/15#discussion_r2903550223)

  Resolved — Dependabot PRs #17–#20 have since landed on `dev`, updating all workflow pins to v5/v6/v7. The changelog entry is now accurate.

---

### Review by [@Copilot](https://github.com/apps/copilot-pull-request-reviewer)

_Posted on March 7, 2026 at 09:28 AM_

_File: [`.github/workflows/ci-container.yml (line 70 RIGHT)`](https://github.com/vig-os/devkit-smoke-test/pull/15#discussion_r2899397138)_

```diff
@@ -0,0 +1,143 @@
+# Container CI Workflow
+#
+# Proof-of-concept: runs CI jobs inside the devcontainer image using the
+# GitHub Actions container: directive.  Only tools already baked into the
+# image are used — no additional installs.
+#
+# Jobs:
+#   1. lint    — Pre-commit hooks (uses image-provided pre-commit + ruff)
+#   2. test    — Pytest with coverage (pytest installed via uv sync)
+#   3. summary — Aggregate results for branch protection
+#
+# Differences from bare-runner ci.yml are documented in
+# docs/container-ci-quirks.md.
+#
+# Triggers:
+#   - Pull requests to dev, release/**, and main
+#   - Manual workflow_dispatch
+#
+
+name: CI (Container)
+
+on:  # yamllint disable-line rule:truthy
+  pull_request:
+    branches:
+      - dev
+      - 'release/**'
+      - main
+  workflow_dispatch:
+    inputs:
+      test-suite:
+        description: 'Test suite to run'
+        required: false
+        default: 'all'
+        type: choice
+        options:
+          - all
+          - lint
+          - test
+  workflow_call:
+    inputs:
+      image-tag:
+        description: 'Container image tag to use'
+        required: false
+        default: 'latest'
+        type: string
+
+permissions:
+  contents: read
+
+jobs:
+  lint:
+    name: Lint & Format (container)
+    runs-on: ubuntu-22.04
+    container:
+      image: ghcr.io/vig-os/devcontainer:${{ inputs.image-tag || 'latest' }}
+      env:
+        # Image-baked cache; may miss if repo pins hooks by hash (see docs/container-ci-quirks.md)
+        PRE_COMMIT_HOME: /opt/pre-commit-cache
+        # Reuse the venv already present in the image instead of creating a new one
+        UV_PROJECT_ENVIRONMENT: /root/assets/workspace/.venv
+    timeout-minutes: 10
+    if: |
+      github.event_name != 'workflow_dispatch' ||
+      inputs.test-suite == 'all' ||
+      inputs.test-suite == 'lint'
+
+    steps:
+      - name: Checkout repository
+        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5  # v4
+
```

This new workflow pins `actions/checkout` to a v4 commit SHA. The PR description/changelog indicate the repo is being promoted with `actions/checkout` bumped to v6.0.2; if that upgrade is intended as part of this dev→main promotion, this workflow should be updated to the same pinned v6 SHA as the rest of the repo to avoid ending up with mixed major versions across workflows.

Conversation:

- **[@c-vigo](https://github.com/c-vigo)** on March 9, 2026 at 06:58 AM — [link](https://github.com/vig-os/devkit-smoke-test/pull/15#discussion_r2903550292)

  Resolved — Dependabot PR #20 has updated `actions/checkout` to v6.0.2 (`de0fac2e...`) on `dev`. The pin mismatch no longer exists.

---

### Review by [@Copilot](https://github.com/apps/copilot-pull-request-reviewer)

_Posted on March 7, 2026 at 09:28 AM_

_File: [`.devcontainer/scripts/check-agent-identity.py`](https://github.com/vig-os/devkit-smoke-test/pull/15#discussion_r2899397147)_

```diff
@@ -0,0 +1,92 @@
+#!/usr/bin/env python3
+"""Reject commits when git author/committer matches AI agent identity blocklist.
+
+Runs in pre-commit stage. Checks GIT_AUTHOR_NAME, GIT_AUTHOR_EMAIL,
+GIT_COMMITTER_NAME, GIT_COMMITTER_EMAIL (from env) and git config user.name/email.
+Skips when running in CI (GITHUB_ACTIONS=true or CI=true).
+
+Refs: #163
+"""
+
+from __future__ import annotations
+
+import os
+import subprocess
+import sys
+from pathlib import Path
+
+
+def _get_git_config(cwd: Path, key: str) -> str:
+    """Get git config value, empty string if not set."""
+    try:
+        result = subprocess.run(
+            ["git", "config", key],
+            cwd=cwd,
+            capture_output=True,
+            text=True,
+            check=False,
+        )
+        return (result.stdout or "").strip()
+    except Exception:
+        return ""
+
+
+def _check_value(value: str, blocklist: dict) -> str | None:
+    """Return matching pattern if value matches blocklist, else None."""
+    if not value:
+        return None
+    value_lower = value.lower()
+    for name in blocklist.get("names", []):
+        if name in value_lower:
+            return name
+    for email in blocklist.get("emails", []):
+        if email in value_lower:
+            return email
+    return None
+
+
+def main() -> int:
+    """Entry point. Exits 1 if author/committer matches blocklist."""
+    if os.environ.get("GITHUB_ACTIONS") == "true" or os.environ.get("CI") == "true":
+        return 0  # Skip in CI; Dependabot etc. use bot identities
+
+    project_root = Path(__file__).resolve().parent.parent
+    blocklist_path = project_root / ".github" / "agent-blocklist.toml"
+    if not blocklist_path.exists():
+        return 0
+
+    from vig_utils.agent_blocklist import load_blocklist
```

`project_root` resolves to the `.devcontainer/` directory, so `blocklist_path` points to `.devcontainer/.github/agent-blocklist.toml` and the check will always short-circuit even though the blocklist is stored at the repo root (`.github/agent-blocklist.toml`). Also, the subsequent import from `vig_utils.agent_blocklist` does not exist in this repository (and is not declared as a dependency), so the script will fail once the blocklist path is corrected. Consider resolving the repo root via `git rev-parse --show-toplevel`/`Path(...).parents[...]` and either vendoring the needed blocklist helper or adding an explicit dependency.

Conversation:

- **[@c-vigo](https://github.com/c-vigo)** on March 9, 2026 at 06:58 AM — [link](https://github.com/vig-os/devkit-smoke-test/pull/15#discussion_r2903550443)

  Valid — filed upstream as [vig-os/devcontainer#238](https://github.com/vig-os/devcontainer/issues/238) (Bugs A + B).

- **[@c-vigo](https://github.com/c-vigo)** on March 9, 2026 at 10:51 AM — [link](https://github.com/vig-os/devkit-smoke-test/pull/15#discussion_r2904656756)

  Implemented in #21

---

### Review by [@Copilot](https://github.com/apps/copilot-pull-request-reviewer)

_Posted on March 7, 2026 at 09:28 AM_

_File: [`.devcontainer/scripts/check-pr-agent-fingerprints.py`](https://github.com/vig-os/devkit-smoke-test/pull/15#discussion_r2899397153)_

```diff
@@ -0,0 +1,41 @@
+#!/usr/bin/env python3
+"""Check PR title and body for AI agent identity fingerprints.
+
+Exits 1 if any blocklisted pattern is found. Used by pr-title-check CI.
+Refs: #163
+"""
+
+from __future__ import annotations
+
+import os
+import sys
+from pathlib import Path
+
+
+def main() -> int:
+    """Entry point. Reads PR_TITLE and PR_BODY from env."""
+    title = os.environ.get("PR_TITLE", "")
+    body = os.environ.get("PR_BODY", "")
+
+    project_root = Path(__file__).resolve().parent.parent
+    blocklist_path = project_root / ".github" / "agent-blocklist.toml"
+    if not blocklist_path.exists():
+        return 0
+
+    from vig_utils.agent_blocklist import contains_agent_fingerprint, load_blocklist
+
+    blocklist = load_blocklist(blocklist_path)
```

`blocklist_path` is built relative to `Path(__file__).parent.parent` (the `.devcontainer/` directory), so it won’t find the repo’s `.github/agent-blocklist.toml` and the check becomes a no-op. Additionally, `vig_utils.agent_blocklist` isn’t present in this repo / lockfile, so this script will error if/when the blocklist path is fixed. Resolve the repo root correctly and either inline the blocklist parsing/matching or add the missing dependency/module.
```suggestion
import re

try:
    import tomllib
except ImportError:  # pragma: no cover - Python < 3.11
    tomllib = None

def load_blocklist(path: Path) -> list[str]:
    """Load blocklisted patterns from a TOML file.

    Supports either:
      - a top-level `patterns = ["regex1", "regex2", ...]` list, or
      - `[[fingerprints]]` tables each with a `pattern = "..."` field.
    """
    if tomllib is None:
        # Environment does not support TOML parsing via tomllib; treat as no blocklist.
        return []

    with path.open("rb") as f:
        data = tomllib.load(f)

    patterns: list[str] = []

    top_level_patterns = data.get("patterns")
    if isinstance(top_level_patterns, list):
        for p in top_level_patterns:
            if isinstance(p, str):
                patterns.append(p)

    fingerprints = data.get("fingerprints")
    if isinstance(fingerprints, list):
        for item in fingerprints:
            if isinstance(item, dict):
                pat = item.get("pattern")
                if isinstance(pat, str):
                    patterns.append(pat)

    return patterns


def contains_agent_fingerprint(content: str, patterns: list[str]) -> str | None:
    """Return the first matching pattern if any fingerprint is found in content."""
    for pattern in patterns:
        try:
            if re.search(pattern, content, flags=re.IGNORECASE):
                return pattern
        except re.error:
            # Skip invalid regex patterns rather than failing the entire check.
            continue
    return None


def main() -> int:
    """Entry point. Reads PR_TITLE and PR_BODY from env."""
    title = os.environ.get("PR_TITLE", "")
    body = os.environ.get("PR_BODY", "")

    # Resolve repository root: <repo>/.devcontainer/scripts/ -> parents[2] == <repo>
    project_root = Path(__file__).resolve().parents[2]
    blocklist_path = project_root / ".github" / "agent-blocklist.toml"
    if not blocklist_path.exists():
        return 0

    blocklist = load_blocklist(blocklist_path)
    if not blocklist:
        return 0
```

Conversation:

- **[@c-vigo](https://github.com/c-vigo)** on March 9, 2026 at 06:58 AM — [link](https://github.com/vig-os/devkit-smoke-test/pull/15#discussion_r2903550533)

  Valid — filed upstream as [vig-os/devcontainer#238](https://github.com/vig-os/devcontainer/issues/238) (Bugs A + B).

- **[@c-vigo](https://github.com/c-vigo)** on March 9, 2026 at 10:52 AM — [link](https://github.com/vig-os/devkit-smoke-test/pull/15#discussion_r2904659968)

  Implemented in #21

---

### Review by [@Copilot](https://github.com/apps/copilot-pull-request-reviewer)

_Posted on March 7, 2026 at 09:28 AM_

_File: [`.devcontainer/scripts/prepare-commit-msg-strip-trailers.py`](https://github.com/vig-os/devkit-smoke-test/pull/15#discussion_r2899397157)_

```diff
@@ -0,0 +1,66 @@
+#!/usr/bin/env python3
+"""Strip AI agent identity trailers from commit message before validation.
+
+Runs in prepare-commit-msg stage. Reads COMMIT_EDITMSG, removes lines matching
+trailer patterns from .github/agent-blocklist.toml, writes back.
+
+Refs: #163
+"""
+
+from __future__ import annotations
+
+import re
+import sys
+from pathlib import Path
+
+
+def _load_trailer_patterns(blocklist_path: Path) -> list[re.Pattern[str]]:
+    """Load trailer regex patterns from blocklist TOML."""
+    import tomllib
+
+    with blocklist_path.open("rb") as f:
+        data = tomllib.load(f)
+    patterns = data.get("patterns", {}).get("trailers", [])
+    return [re.compile(p) for p in patterns]
+
+
+def strip_trailers(msg_path: Path, blocklist_path: Path) -> bool:
+    """Remove lines matching trailer patterns. Returns True if any line was removed."""
+    patterns = _load_trailer_patterns(blocklist_path)
+    content = msg_path.read_text(encoding="utf-8", errors="replace")
+    lines = content.splitlines(keepends=True)
+    new_lines: list[str] = []
+    changed = False
+    for line in lines:
+        stripped = line.rstrip("\n\r")
+        if any(p.match(stripped) for p in patterns):
+            changed = True
+            continue
+        new_lines.append(line)
+    if changed:
+        msg_path.write_text("".join(new_lines), encoding="utf-8")
+    return changed
+
+
+def main() -> int:
+    """Entry point. Expects COMMIT_EDITMSG path as first arg."""
+    if len(sys.argv) < 2:
+        print(
+            "Usage: prepare-commit-msg-strip-trailers <path-to-COMMIT_EDITMSG>",
+            file=sys.stderr,
+        )
+        return 2
+    msg_path = Path(sys.argv[1])
+    project_root = Path(__file__).resolve().parent.parent
+    blocklist_path = project_root / ".github" / "agent-blocklist.toml"
+    if not blocklist_path.exists():
```

`project_root` resolves to `.devcontainer/`, so `blocklist_path` points to `.devcontainer/.github/agent-blocklist.toml` and this hook will silently skip stripping trailers even when the blocklist exists at the repo root (`.github/agent-blocklist.toml`). Resolve the repository root before constructing the blocklist path so the hook actually enforces the configured trailer patterns.

Conversation:

- **[@c-vigo](https://github.com/c-vigo)** on March 9, 2026 at 06:58 AM — [link](https://github.com/vig-os/devkit-smoke-test/pull/15#discussion_r2903550638)

  Valid — filed upstream as [vig-os/devcontainer#238](https://github.com/vig-os/devcontainer/issues/238) (Bug A).

- **[@c-vigo](https://github.com/c-vigo)** on March 9, 2026 at 10:52 AM — [link](https://github.com/vig-os/devkit-smoke-test/pull/15#discussion_r2904661073)

  Implemented in #21


---
---

## Commits

### Commit 1: [9921183](https://github.com/vig-os/devkit-smoke-test/commit/99211834dddca4e648839bdb8517301ec2ba27b0) by [commit-action-bot[bot]](https://github.com/apps/commit-action-bot) on February 26, 2026 at 04:17 AM
chore: sync issues and PRs, 473 files modified (docs/issues/issue-1.md, docs/issues/issue-3.md, docs/issues/issue-6.md, docs/pull-requests/pr-2.md, docs/pull-requests/pr-4.md, docs/pull-requests/pr-5.md)

### Commit 2: [911aa95](https://github.com/vig-os/devkit-smoke-test/commit/911aa95014e419e119b30064225daf3e40095516) by [commit-action-bot[bot]](https://github.com/apps/commit-action-bot) on February 27, 2026 at 04:13 AM
chore: sync issues and PRs, 6 files modified (docs/pull-requests/pr-5.md)

### Commit 3: [b458bf8](https://github.com/vig-os/devkit-smoke-test/commit/b458bf88cf5b53cad238b9ab3a56eb92c1c3355c) by [dependabot[bot]](https://github.com/apps/dependabot) on March 2, 2026 at 05:30 AM
ci(deps): bump the actions-minor-patch group with 3 updates, 10 files modified (.github/workflows/ci.yml, .github/workflows/codeql.yml, .github/workflows/scorecard.yml, .github/workflows/sync-issues.yml)

### Commit 4: [8cfe567](https://github.com/vig-os/devkit-smoke-test/commit/8cfe567bfcdd824034e4f5f0a1e724d1999343c5) by [dependabot[bot]](https://github.com/apps/dependabot) on March 2, 2026 at 05:30 AM
ci(deps): bump actions/upload-artifact from 4.6.2 to 7.0.0, 4 files modified (.github/workflows/ci.yml)

### Commit 5: [2d13115](https://github.com/vig-os/devkit-smoke-test/commit/2d13115ebc020683369df5c632ba9bd89c358640) by [dependabot[bot]](https://github.com/apps/dependabot) on March 2, 2026 at 05:30 AM
ci(deps): bump actions/checkout from 4.3.1 to 6.0.2, 24 files modified (.github/workflows/ci.yml, .github/workflows/codeql.yml, .github/workflows/release.yml, .github/workflows/scorecard.yml, .github/workflows/sync-issues.yml)

### Commit 6: [cfafee7](https://github.com/vig-os/devkit-smoke-test/commit/cfafee728fee4a768825cd9b480b011c00878d86) by [dependabot[bot]](https://github.com/apps/dependabot) on March 2, 2026 at 05:30 AM
ci(deps): bump actions/cache from 4.3.0 to 5.0.3, 4 files modified (.github/workflows/sync-issues.yml)

### Commit 7: [7db9b17](https://github.com/vig-os/devkit-smoke-test/commit/7db9b17ffa20b2a0adcccdfd53524ad3652657e5) by [commit-action-bot[bot]](https://github.com/apps/commit-action-bot) on March 3, 2026 at 04:13 AM
chore: sync issues and PRs, 828 files modified (docs/pull-requests/pr-10.md, docs/pull-requests/pr-7.md, docs/pull-requests/pr-8.md, docs/pull-requests/pr-9.md)

### Commit 8: [b055df2](https://github.com/vig-os/devkit-smoke-test/commit/b055df2e13b9ff6d251f54c9dcc7821582804c10) by [c-vigo](https://github.com/c-vigo) on March 3, 2026 at 10:03 AM
ci(deps): bump actions/checkout from 4.3.1 to 6.0.2 (#9), 24 files modified (.github/workflows/ci.yml, .github/workflows/codeql.yml, .github/workflows/release.yml, .github/workflows/scorecard.yml, .github/workflows/sync-issues.yml)

### Commit 9: [f68b7f7](https://github.com/vig-os/devkit-smoke-test/commit/f68b7f7d25fb21248054528bf48caa1e320d7f68) by [c-vigo](https://github.com/c-vigo) on March 3, 2026 at 10:03 AM
ci(deps): bump actions/cache from 4.3.0 to 5.0.3 (#10), 4 files modified (.github/workflows/sync-issues.yml)

### Commit 10: [b2d7112](https://github.com/vig-os/devkit-smoke-test/commit/b2d71127ee9c8b39a5e4374d19a3ce88d27b6b1e) by [c-vigo](https://github.com/c-vigo) on March 3, 2026 at 10:04 AM
ci(deps): bump the actions-minor-patch group with 3 updates (#7), 10 files modified (.github/workflows/ci.yml, .github/workflows/codeql.yml, .github/workflows/scorecard.yml, .github/workflows/sync-issues.yml)

### Commit 11: [accb895](https://github.com/vig-os/devkit-smoke-test/commit/accb8951f65e15a56947e5941d586a9b56591333) by [c-vigo](https://github.com/c-vigo) on March 3, 2026 at 10:04 AM
ci(deps): bump actions/upload-artifact from 4.6.2 to 7.0.0 (#8), 4 files modified (.github/workflows/ci.yml)

### Commit 12: [5a2515f](https://github.com/vig-os/devkit-smoke-test/commit/5a2515f2798e7105ef16e3fd0ff69bcb0b82f2b8) by [c-vigo](https://github.com/c-vigo) on March 3, 2026 at 10:08 AM
docs(changelog): add notes for merged Dependabot Actions updates, 4 files modified (CHANGELOG.md)

### Commit 13: [cd88d0c](https://github.com/vig-os/devkit-smoke-test/commit/cd88d0c7c3cacc61745ac2c3c3d5763776bd2f12) by [c-vigo](https://github.com/c-vigo) on March 3, 2026 at 10:10 AM
chore: update devcontainer image and template, 538 files modified

### Commit 14: [c1c12c9](https://github.com/vig-os/devkit-smoke-test/commit/c1c12c939a166173ccb1ae22f3412c99666c0514) by [c-vigo](https://github.com/c-vigo) on March 3, 2026 at 11:59 AM
chore: update devcontainer image and template (#11), 542 files modified

### Commit 15: [8f1d559](https://github.com/vig-os/devkit-smoke-test/commit/8f1d559d1f61d2c8b527f3c4d3c6f75c13e5a589) by [c-vigo](https://github.com/c-vigo) on March 3, 2026 at 05:29 PM
chore: update devcontainer image and template, 208 files modified (.github/workflows/ci-container.yml, .pre-commit-config.yaml, docs/container-ci-quirks.md)

### Commit 16: [2e59be3](https://github.com/vig-os/devkit-smoke-test/commit/2e59be3d46d9cbff4fa50c2197b1847f6d68637a) by [c-vigo](https://github.com/c-vigo) on March 3, 2026 at 05:56 PM
docs: update CHANGELOG with ci-container entry, 3 files modified (CHANGELOG.md)

### Commit 17: [c25db9e](https://github.com/vig-os/devkit-smoke-test/commit/c25db9e3cf0c185d291ab1f53013e1b3fd3ab209) by [c-vigo](https://github.com/c-vigo) on March 3, 2026 at 07:59 PM
chore: add ci-container validation workflow and quirks docs (#12), 211 files modified (.github/workflows/ci-container.yml, .pre-commit-config.yaml, CHANGELOG.md, docs/container-ci-quirks.md)

### Commit 18: [5d91ada](https://github.com/vig-os/devkit-smoke-test/commit/5d91ada201e5bbee58b74552f19c9f535569abb3) by [c-vigo](https://github.com/c-vigo) on March 7, 2026 at 07:23 AM
ci: parameterize container workflow image tag, 11 files modified (.github/workflows/ci-container.yml)

### Commit 19: [db5d2df](https://github.com/vig-os/devkit-smoke-test/commit/db5d2df63f7283d32bb00a7305de5fbcb318b2fe) by [c-vigo](https://github.com/c-vigo) on March 7, 2026 at 07:23 AM
ci: add workflow_call trigger to bare-runner CI, 1 file modified (.github/workflows/ci.yml)

### Commit 20: [a4a68e6](https://github.com/vig-os/devkit-smoke-test/commit/a4a68e62e380090d5be01afb6181d699e4c2e36c) by [c-vigo](https://github.com/c-vigo) on March 7, 2026 at 07:23 AM
ci: wire repository_dispatch to run smoke CI variants, 71 files modified (.github/workflows/repository-dispatch.yml)

### Commit 21: [980aaa5](https://github.com/vig-os/devkit-smoke-test/commit/980aaa53933097ae92ae08247d1f54aed03b4567) by [c-vigo](https://github.com/c-vigo) on March 7, 2026 at 08:36 AM
docs: update CHANGELOG, 4 files modified (CHANGELOG.md)

### Commit 22: [5382255](https://github.com/vig-os/devkit-smoke-test/commit/5382255302f3edf468faace6ec153107fe8203f6) by [c-vigo](https://github.com/c-vigo) on March 7, 2026 at 08:41 AM
ci: wire smoke-test dispatch to run RC CI variants (#14), 87 files modified (.github/workflows/ci-container.yml, .github/workflows/ci.yml, .github/workflows/repository-dispatch.yml, CHANGELOG.md)

### Commit 23: [8f78a6a](https://github.com/vig-os/devkit-smoke-test/commit/8f78a6ad070bcefbd794e86dee4a5060af2e9361) by [commit-action-bot[bot]](https://github.com/apps/commit-action-bot) on March 8, 2026 at 04:10 AM
chore: sync issues and PRs, 873 files modified (docs/issues/issue-13.md, docs/pull-requests/pr-11.md, docs/pull-requests/pr-14.md, docs/pull-requests/pr-15.md)

### Commit 24: [ce86cab](https://github.com/vig-os/devkit-smoke-test/commit/ce86cabef2a89b29dc58ae5af876e1f8bb8c2a03) by [commit-action-bot[bot]](https://github.com/apps/commit-action-bot) on March 9, 2026 at 04:17 AM
chore: sync issues and PRs, 91 files modified (docs/issues/issue-16.md)

### Commit 25: [603e635](https://github.com/vig-os/devkit-smoke-test/commit/603e6353fa055769773d415db3c54649f546446b) by [dependabot[bot]](https://github.com/apps/dependabot) on March 9, 2026 at 05:32 AM
ci(deps): bump the actions-minor-patch group with 3 updates, 10 files modified (.github/workflows/ci.yml, .github/workflows/codeql.yml, .github/workflows/scorecard.yml, .github/workflows/sync-issues.yml)

### Commit 26: [59bc1b7](https://github.com/vig-os/devkit-smoke-test/commit/59bc1b7d252de25329a04bd88bdd878a86fb304a) by [dependabot[bot]](https://github.com/apps/dependabot) on March 9, 2026 at 05:32 AM
ci(deps): bump actions/upload-artifact from 4.6.2 to 7.0.0, 4 files modified (.github/workflows/ci.yml)

### Commit 27: [516dd77](https://github.com/vig-os/devkit-smoke-test/commit/516dd7794319f16fbd6c8d47af794f693294c4a1) by [dependabot[bot]](https://github.com/apps/dependabot) on March 9, 2026 at 05:32 AM
ci(deps): bump actions/cache from 4.3.0 to 5.0.3, 4 files modified (.github/workflows/sync-issues.yml)

### Commit 28: [bf0959f](https://github.com/vig-os/devkit-smoke-test/commit/bf0959f0130646b9571cf897f5c7c5a1adbec5b3) by [dependabot[bot]](https://github.com/apps/dependabot) on March 9, 2026 at 05:33 AM
ci(deps): bump actions/checkout from 4.3.1 to 6.0.2, 28 files modified (.github/workflows/ci-container.yml, .github/workflows/ci.yml, .github/workflows/codeql.yml, .github/workflows/release.yml, .github/workflows/scorecard.yml, .github/workflows/sync-issues.yml)

### Commit 29: [8026050](https://github.com/vig-os/devkit-smoke-test/commit/8026050337b48daceab495b056ece69f9aa1ff02) by [c-vigo](https://github.com/c-vigo) on March 9, 2026 at 06:52 AM
ci(deps): bump actions/checkout from 4.3.1 to 6.0.2 (#20), 28 files modified (.github/workflows/ci-container.yml, .github/workflows/ci.yml, .github/workflows/codeql.yml, .github/workflows/release.yml, .github/workflows/scorecard.yml, .github/workflows/sync-issues.yml)

### Commit 30: [1406566](https://github.com/vig-os/devkit-smoke-test/commit/14065667db4259aef3d4a046346e317081a4ab2c) by [c-vigo](https://github.com/c-vigo) on March 9, 2026 at 06:53 AM
ci(deps): bump actions/upload-artifact from 4.6.2 to 7.0.0 (#18), 4 files modified (.github/workflows/ci.yml)

### Commit 31: [d554147](https://github.com/vig-os/devkit-smoke-test/commit/d5541471c490a75024e8f97d12981876760ae567) by [c-vigo](https://github.com/c-vigo) on March 9, 2026 at 06:53 AM
ci(deps): bump actions/cache from 4.3.0 to 5.0.3 (#19), 4 files modified (.github/workflows/sync-issues.yml)

### Commit 32: [91dee09](https://github.com/vig-os/devkit-smoke-test/commit/91dee09a01c2257f045b67372fe0cd481d0fe7e6) by [c-vigo](https://github.com/c-vigo) on March 9, 2026 at 06:54 AM
ci(deps): bump the actions-minor-patch group with 3 updates (#17), 10 files modified (.github/workflows/ci.yml, .github/workflows/codeql.yml, .github/workflows/scorecard.yml, .github/workflows/sync-issues.yml)

### Commit 33: [15b7d50](https://github.com/vig-os/devkit-smoke-test/commit/15b7d503c390127ac5cfc4e16da52dae96a699f1) by [c-vigo](https://github.com/c-vigo) on March 9, 2026 at 10:36 AM
chore: update devcontainer image and template, 1636 files modified

### Commit 34: [9bccd87](https://github.com/vig-os/devkit-smoke-test/commit/9bccd87329a0bcb66e493c6c2d175be07ee33ba4) by [c-vigo](https://github.com/c-vigo) on March 9, 2026 at 10:46 AM
docs: update CHANGELOG, 3 files modified (CHANGELOG.md)

### Commit 35: [ac31d8a](https://github.com/vig-os/devkit-smoke-test/commit/ac31d8a540cd8d11b633ab06fdeec482671298e7) by [c-vigo](https://github.com/c-vigo) on March 9, 2026 at 12:42 PM
chore: update devcontainer image and template, 81 files modified (.devcontainer/justfile.devc, .devcontainer/justfile.gh, .devcontainer/justfile.worktree, .github/label-taxonomy.toml, .vscode/settings.json, CHANGELOG.md, justfile.project, uv.lock)

### Commit 36: [6431db6](https://github.com/vig-os/devkit-smoke-test/commit/6431db66cb29243bff41fcff750dfdb3bf4ec453) by [c-vigo](https://github.com/c-vigo) on March 9, 2026 at 02:02 PM
chore: update devcontainer image and template (#21), 1700 files modified
