---
type: pull_request
state: closed (merged)
branch: feature/13-wire-smoke-dispatch-rc-ci → dev
created: 2026-03-09T10:49:18Z
updated: 2026-03-09T14:02:27Z
author: c-vigo
author_url: https://github.com/c-vigo
url: https://github.com/vig-os/devkit-smoke-test/pull/21
comments: 15
labels: none
assignees: c-vigo
milestone: none
projects: none
merged: 2026-03-09T14:02:21Z
synced: 2026-07-10T15:45:37.300Z
---

# [PR 21](https://github.com/vig-os/devkit-smoke-test/pull/21) chore: update devcontainer image and template

## Description

Refreshes the devcontainer template baseline to the latest upstream snapshot and aligns supporting hooks/tooling metadata with that update.

## Type of Change

- [ ] `feat` -- New feature
- [ ] `fix` -- Bug fix
- [ ] `docs` -- Documentation only
- [x] `chore` -- Maintenance task (deps, config, etc.)
- [ ] `refactor` -- Code restructuring (no behavior change)
- [ ] `test` -- Adding or updating tests
- [ ] `ci` -- CI/CD pipeline changes
- [ ] `build` -- Build system or dependency changes
- [ ] `revert` -- Reverts a previous commit
- [ ] `style` -- Code style (formatting, whitespace)

### Modifiers

- [ ] Breaking change (`!`) -- This change breaks backward compatibility

## Changes Made

- `.devcontainer/justfile.devc`: adds the current shared devcontainer task definitions used by bootstrap and validation flows.
- `.devcontainer/scripts/*`: removes deprecated helper scripts that are no longer part of the active template workflow.
- `pyproject.toml` and `uv.lock`: syncs Python tooling/dependency lock state with the refreshed template.
- `.githooks/*`, `justfile.project`, and related config files: updates hook wiring and template metadata to match the upstream baseline.

## Changelog Entry

### Changed

- **Updated devcontainer template baseline to current upstream snapshot** ([#13](https://github.com/vig-os/devcontainer-smoke-test/issues/13))
  - Synced devcontainer helper scripts, hook wiring, and lock/tooling metadata with the latest template revision
  - Removed deprecated bootstrap helper scripts no longer used by the local and CI developer workflow

## Testing

- [x] Tests pass locally (`just test`)
- [ ] Manual testing performed (describe below)

### Manual Testing Details

N/A

## Checklist

- [ ] My code follows the project's style guidelines
- [x] I have performed a self-review of my code
- [ ] I have commented my code, particularly in hard-to-understand areas
- [ ] I have updated the documentation accordingly (edit `docs/templates/`, then run `just docs`)
- [x] I have updated `CHANGELOG.md` in the `[Unreleased]` section (and pasted the entry above)
- [ ] My changes generate no new warnings or errors
- [ ] I have added tests that prove my fix is effective or that my feature works
- [x] New and existing unit tests pass locally with my changes
- [ ] Any dependent changes have been merged and published

## Additional Notes

N/A

Refs: #13



---
---

## Review Threads (7)

### Review by [@Copilot](https://github.com/apps/copilot-pull-request-reviewer)

_Posted on March 9, 2026 at 10:58 AM_

_File: [`.github/label-taxonomy.toml (line 11 RIGHT)`](https://github.com/vig-os/devkit-smoke-test/pull/21#discussion_r2904688843)_

```diff
@@ -1,12 +1,12 @@
 # Canonical repository labels.
 # Single source of truth — referenced by:
-#   - scripts/setup-labels.sh               (provision labels on a repo)
+#   - uv run setup-labels                   (provision labels on a repo)
 #   - .cursor/skills/issue_triage/SKILL.md  (triage label check)
 #   - .cursor/skills/issue_create/SKILL.md  (agent label mapping)
 #   - .github/ISSUE_TEMPLATE/*.yml          (template label values)
 #
 # Label reconciliation:
-#   Run `scripts/setup-labels.sh` after repo creation to create/update labels
+#   Run `uv run setup-labels` after repo creation to create/update labels
 #   from this taxonomy. Use `--prune` to remove org-default labels that don't
```

The header comment now instructs running `uv run setup-labels`, but this repository doesn’t contain a `setup-labels` script/module and it’s not present in the lockfile as an installable dependency. Unless this command is guaranteed to be available from the devcontainer image, these instructions are likely to break for users. Consider either restoring/adding a `setup-labels` implementation (and documenting where it comes from) or reverting to a repo-local script reference.

Conversation:

- **[@c-vigo](https://github.com/c-vigo)** on March 9, 2026 at 12:43 PM — [link](https://github.com/vig-os/devkit-smoke-test/pull/21#discussion_r2905198347)

  Addressed in 1af5337. The comment now explicitly documents that `setup-labels` is provided by devcontainer bootstrap tooling, so the `uv run setup-labels` instruction is intentional and traceable.

---

### Review by [@Copilot](https://github.com/apps/copilot-pull-request-reviewer)

_Posted on March 9, 2026 at 10:58 AM_

_File: [`.devcontainer/justfile.worktree (line 150 RIGHT)`](https://github.com/vig-os/devkit-smoke-test/pull/21#discussion_r2904688887)_

```diff
@@ -139,7 +136,7 @@ worktree-start issue prompt="" reviewer="":
     fi
 
     # Resolve the issue's linked branch (may already exist from issue:claim)
-    BRANCH=$(gh issue develop --list "$ISSUE" 2>/dev/null | "{{ _scripts }}/resolve-branch.sh")
+    BRANCH=$(gh issue develop --list "$ISSUE" 2>/dev/null | resolve-branch)
```

`worktree-start` now pipes `gh issue develop --list` into `resolve-branch`, but this repo no longer contains a `resolve-branch` script/recipe and there’s no other definition in-tree. If the devcontainer image doesn’t provide this executable on PATH, branch resolution will fail here. Consider either restoring a repo-local implementation (e.g., under `.devcontainer/scripts/`) or adding an explicit availability check with a clear error message + fallback parsing (e.g., `head -1 | cut -f1`).

Conversation:

- **[@c-vigo](https://github.com/c-vigo)** on March 9, 2026 at 12:43 PM — [link](https://github.com/vig-os/devkit-smoke-test/pull/21#discussion_r2905198435)

  Addressed in 1af5337. `worktree-start` now checks `resolve-branch --help` up front and fails fast with install guidance instead of failing later during branch resolution.

---

### Review by [@Copilot](https://github.com/apps/copilot-pull-request-reviewer)

_Posted on March 9, 2026 at 10:58 AM_

_File: [`.devcontainer/justfile.worktree (line 173 RIGHT)`](https://github.com/vig-os/devkit-smoke-test/pull/21#discussion_r2904688909)_

```diff
@@ -159,11 +156,11 @@ worktree-start issue prompt="" reviewer="":
         # Use agent ONLY for the intelligent part: deriving the short summary
         # Try lightweight first; on failure retry with standard model (#183)
         NAMING_RULE="$(pwd)/.cursor/rules/branch-naming.mdc"
-        SUMMARY=$("{{ _scripts }}/derive-branch-summary.sh" "$TITLE" "$NAMING_RULE" "lightweight") || true
+        SUMMARY=$(derive-branch-summary "$TITLE" "$NAMING_RULE" "lightweight") || true
 
         if [ -z "$SUMMARY" ]; then
             echo "[!] Lightweight model failed. Retrying with standard model..."
-            SUMMARY=$("{{ _scripts }}/derive-branch-summary.sh" "$TITLE" "$NAMING_RULE" "standard") || true
+            SUMMARY=$(derive-branch-summary "$TITLE" "$NAMING_RULE" "standard") || true
```

`derive-branch-summary` is invoked as a command, but there’s no script/recipe for it in the repository anymore. Unless this is guaranteed to be installed by the devcontainer image, `worktree-start` will always fall back to the manual-branch path. Consider restoring the removed `.devcontainer/scripts/derive-branch-summary.sh` (or equivalent) or checking for the command’s existence early and emitting guidance on how to install/enable it.

Conversation:

- **[@c-vigo](https://github.com/c-vigo)** on March 9, 2026 at 12:43 PM — [link](https://github.com/vig-os/devkit-smoke-test/pull/21#discussion_r2905198509)

  Addressed in 1af5337. Added an explicit preflight check for `derive-branch-summary --help` with a clear bootstrap/sync instruction if the helper CLI is unavailable.

---

### Review by [@Copilot](https://github.com/apps/copilot-pull-request-reviewer)

_Posted on March 9, 2026 at 10:58 AM_

_File: [`.devcontainer/justfile.worktree (line 186 RIGHT)`](https://github.com/vig-os/devkit-smoke-test/pull/21#discussion_r2904688938)_

```diff
@@ -175,7 +172,7 @@ worktree-start issue prompt="" reviewer="":
         OWNER_REPO=$(gh repo view --json nameWithOwner --jq '.nameWithOwner')
         PARENT=$(gh api "repos/${OWNER_REPO}/issues/${ISSUE}/parent" --jq '.number' 2>/dev/null || true)
         if [ -n "$PARENT" ]; then
-            BASE=$(gh issue develop --list "$PARENT" 2>/dev/null | "{{ _scripts }}/resolve-branch.sh")
+            BASE=$(gh issue develop --list "$PARENT" 2>/dev/null | resolve-branch)
             BASE="${BASE:-dev}"
```

This second `resolve-branch` usage (to determine the parent issue’s base branch) has the same dependency on an external `resolve-branch` executable that isn’t defined in this repo. If it’s missing, base-branch inference will silently degrade (and may default incorrectly). Consider keeping the parsing logic in-repo or adding a `command -v resolve-branch` guard before first use.

Conversation:

- **[@c-vigo](https://github.com/c-vigo)** on March 9, 2026 at 12:43 PM — [link](https://github.com/vig-os/devkit-smoke-test/pull/21#discussion_r2905198594)

  Addressed in 1af5337 via the same preflight guard for `resolve-branch`, which now validates command availability once before both call sites and exits with guidance when missing.

---

### Review by [@Copilot](https://github.com/apps/copilot-pull-request-reviewer)

_Posted on March 9, 2026 at 10:58 AM_

_File: [`.devcontainer/justfile.gh (line 20 RIGHT)`](https://github.com/vig-os/devkit-smoke-test/pull/21#discussion_r2904688959)_

```diff
@@ -8,9 +8,7 @@
 
 alias gh-i := gh-issues
 
-_gh_scripts := source_directory() / "scripts"
-
 # List open issues and PRs grouped by milestone
 [group('github')]
 gh-issues:
-    python3 {{ _gh_scripts }}/gh_issues.py
+    gh-issues
```

The `gh-issues` recipe now runs an external `gh-issues` command, but the previous in-repo implementation (`.devcontainer/scripts/gh_issues.py`) was removed and there’s no other definition in this repo. If the devcontainer image doesn’t ship a `gh-issues` executable, this recipe will fail. Consider keeping a repo-local fallback (or renaming the external command to avoid ambiguity) and/or adding a presence check with a helpful error.

Conversation:

- **[@c-vigo](https://github.com/c-vigo)** on March 9, 2026 at 12:43 PM — [link](https://github.com/vig-os/devkit-smoke-test/pull/21#discussion_r2905198675)

  Addressed in 1af5337. The `gh-issues` recipe now validates `gh-issues --help` first and exits with a clear install path (workspace/bootstrap sync) when the helper CLI is not present.

---

### Review by [@Copilot](https://github.com/apps/copilot-pull-request-reviewer)

_Posted on March 9, 2026 at 10:58 AM_

_File: [`justfile.project`](https://github.com/vig-os/devkit-smoke-test/pull/21#discussion_r2904688988)_

```diff
@@ -18,6 +17,82 @@ project := "devcontainer_smoke_test"
 info:
     @echo "Project: {{ project }}"
 
+# -------------------------------------------------------------------------------
+# CODE QUALITY
+# -------------------------------------------------------------------------------
+
+# Run all linters
+[group('quality')]
+lint:
+    uv run ruff check .
+
+# Format code
+[group('quality')]
+format:
+    uv run ruff format .
+
+# Run pre-commit hooks on all files
+[group('quality')]
+precommit:
+    uv run pre-commit run --all-files
+
+# -------------------------------------------------------------------------------
+# TESTING
+# -------------------------------------------------------------------------------
+
+# Run tests with pytest
+[group('test')]
+test *args:
+    uv run pytest {{ args }}
+
+# Run tests with coverage
+[group('test')]
+test-cov *args:
+    uv run pytest --cov --cov-report=term-missing {{ args }}
+
+# -------------------------------------------------------------------------------
+# DEPENDENCIES
+# -------------------------------------------------------------------------------
+
+# Sync all dependencies (idempotent, fast if nothing changed)
+[group('deps')]
+sync:
+    @if [ -f pyproject.toml ]; then uv sync --all-extras; fi
```

`sync` uses `uv sync --all-extras`, but this repo now defines a `[dependency-groups].dev` (hatchling, rich). `--all-extras` won’t install dependency groups, so `just sync` won’t actually sync the newly-added dev tooling group. Consider updating sync to include the intended groups (e.g., `--group dev` / `--all-groups`) or moving these requirements into optional-dependencies if they’re meant to be covered by `--all-extras`.

Conversation:

- **[@c-vigo](https://github.com/c-vigo)** on March 9, 2026 at 01:57 PM — [link](https://github.com/vig-os/devkit-smoke-test/pull/21#discussion_r2905596947)

  Re-checked on the latest PR commit (`dce003d`): this is still a valid gap. `just sync` currently uses `uv sync --all-extras`, while `pyproject.toml` also defines `[dependency-groups].dev`, so group deps are not installed by that recipe yet. I am leaving this thread open for a follow-up fix (or can patch it here if we decide to include it in this PR).

- **[@c-vigo](https://github.com/c-vigo)** on March 9, 2026 at 02:00 PM — [link](https://github.com/vig-os/devkit-smoke-test/pull/21#discussion_r2905613846)

  Addressed in `ac31d8a`. `justfile.project` now uses `uv sync --all-extras --all-groups`, so `just sync` installs both optional extras and dependency groups (including `[dependency-groups].dev`).

---

### Review by [@Copilot](https://github.com/apps/copilot-pull-request-reviewer)

_Posted on March 9, 2026 at 10:58 AM_

_File: [`.devcontainer/justfile.devc (line 244 RIGHT)`](https://github.com/vig-os/devkit-smoke-test/pull/21#discussion_r2904689017)_

```diff
@@ -0,0 +1,289 @@
+# ===============================================================================
+# DEVCONTAINER RECIPES - DO NOT EDIT
+# Managed by vigOS devcontainer. Customizations go in root justfile.
+# ===============================================================================
+
+set shell := ["bash", "-euo", "pipefail", "-c"]
+
+# -------------------------------------------------------------------------------
+# VERSION CHECK
+# -------------------------------------------------------------------------------
+
+# Check for devcontainer updates (verbose)
+# Usage: just check [config|on|off|<duration>]
+# Examples: just check, just check config, just check off, just check 7d
+[group('info')]
+check *args:
+    #!/usr/bin/env bash
+    SCRIPT_DIR="$(cd "{{source_directory()}}/scripts" && pwd)" || {
+        echo "Error: Could not locate .devcontainer/scripts directory"
+        exit 1
+    }
+
+    if [[ ! -f "$SCRIPT_DIR/version-check.sh" ]]; then
+        echo "Error: version-check.sh not found at $SCRIPT_DIR"
+        exit 1
+    fi
+
+    # No args provided: default to verbose check mode.
+    # Otherwise pass all args through as provided by just.
+    "$SCRIPT_DIR/version-check.sh" {{ if args == '' { 'check' } else { args } }}
+
+# Upgrade devcontainer to latest version (host-side only)
+# This recipe MUST be run from a host terminal, not inside the container
+[group('info')]
+devcontainer-upgrade:
+    #!/usr/bin/env bash
+    # Detect if running inside a container
+    if [[ -f "/.dockerenv" ]] || [[ -n "${container:-}" ]]; then
+        echo ""
+        echo "❌ ERROR: This command must be run from a HOST terminal"
+        echo ""
+        echo "You are currently inside the devcontainer."
+        echo "Open a new terminal on your host machine and run:"
+        echo ""
+        echo "    cd $(pwd)"
+        echo "    just devcontainer-upgrade"
+        echo ""
+        echo "Or use the curl method:"
+        echo ""
+        echo "    curl -sSf https://vig-os.github.io/devcontainer/install.sh | sh -s -- --force ."
+        echo ""
+        exit 1
+    fi
+
+    # We're on the host - proceed with upgrade
+    echo "🚀 Upgrading devcontainer to latest version..."
+    echo ""
+
+    # Check if podman or docker is available
+    if command -v podman &> /dev/null; then
+        RUNTIME="podman"
+    elif command -v docker &> /dev/null; then
+        RUNTIME="docker"
+    else
+        echo "❌ ERROR: Neither podman nor docker is installed"
+        echo ""
+        echo "Please install one of them first:"
+        echo "  macOS:  brew install podman && podman machine init && podman machine start"
+        echo "  Linux:  sudo apt install podman   # or equivalent for your distro"
+        echo ""
+        exit 1
+    fi
+
+    echo "✓ Using $RUNTIME"
+    echo ""
+    echo "Downloading and running install script..."
+    curl -sSf https://vig-os.github.io/devcontainer/install.sh | sh -s -- --force .
+
+# -------------------------------------------------------------------------------
+# DEVCONTAINER (host-side only)
+# -------------------------------------------------------------------------------
+# These recipes must be run from the host, not inside the container.
+# Compose command: podman compose preferred, fallback to docker compose.
+
+# Start devcontainer + sidecars via compose
+[group('devcontainer')]
+up:
+    #!/usr/bin/env bash
+    if [[ -f "/.dockerenv" ]] || [[ -n "${container:-}" ]]; then
+        echo "[ERROR] Run this from the host: just up"
+        exit 1
+    fi
+    cd .devcontainer || exit 1
+    COMPOSE_OPTS="-f docker-compose.yml -f docker-compose.project.yaml"
+    [ -f docker-compose.local.yaml ] && COMPOSE_OPTS="$COMPOSE_OPTS -f docker-compose.local.yaml"
+    if command -v podman &>/dev/null && podman compose version &>/dev/null 2>&1; then
+        podman compose $COMPOSE_OPTS up -d
+    elif command -v docker &>/dev/null && docker compose version &>/dev/null 2>&1; then
+        docker compose $COMPOSE_OPTS up -d
+    else
+        echo "[ERROR] Need podman or docker with compose"
+        exit 1
+    fi
+
+# Stop and remove containers
+[group('devcontainer')]
+down:
+    #!/usr/bin/env bash
+    if [[ -f "/.dockerenv" ]] || [[ -n "${container:-}" ]]; then
+        echo "[ERROR] Run this from the host: just down"
+        exit 1
+    fi
+    cd .devcontainer || exit 1
+    COMPOSE_OPTS="-f docker-compose.yml -f docker-compose.project.yaml"
+    [ -f docker-compose.local.yaml ] && COMPOSE_OPTS="$COMPOSE_OPTS -f docker-compose.local.yaml"
+    if command -v podman &>/dev/null && podman compose version &>/dev/null 2>&1; then
+        podman compose $COMPOSE_OPTS down
+    elif command -v docker &>/dev/null && docker compose version &>/dev/null 2>&1; then
+        docker compose $COMPOSE_OPTS down
+    else
+        echo "[ERROR] Need podman or docker with compose"
+        exit 1
+    fi
+
+# Show container status
+[group('devcontainer')]
+status:
+    #!/usr/bin/env bash
+    if [[ -f "/.dockerenv" ]] || [[ -n "${container:-}" ]]; then
+        echo "[ERROR] Run this from the host: just status"
+        exit 1
+    fi
+    cd .devcontainer || exit 1
+    COMPOSE_OPTS="-f docker-compose.yml -f docker-compose.project.yaml"
+    [ -f docker-compose.local.yaml ] && COMPOSE_OPTS="$COMPOSE_OPTS -f docker-compose.local.yaml"
+    if command -v podman &>/dev/null && podman compose version &>/dev/null 2>&1; then
+        podman compose $COMPOSE_OPTS ps
+    elif command -v docker &>/dev/null && docker compose version &>/dev/null 2>&1; then
+        docker compose $COMPOSE_OPTS ps
+    else
+        echo "[ERROR] Need podman or docker with compose"
+        exit 1
+    fi
+
+# Tail container logs
+[group('devcontainer')]
+logs *args:
+    #!/usr/bin/env bash
+    if [[ -f "/.dockerenv" ]] || [[ -n "${container:-}" ]]; then
+        echo "[ERROR] Run this from the host: just logs"
+        exit 1
+    fi
+    cd .devcontainer || exit 1
+    COMPOSE_OPTS="-f docker-compose.yml -f docker-compose.project.yaml"
+    [ -f docker-compose.local.yaml ] && COMPOSE_OPTS="$COMPOSE_OPTS -f docker-compose.local.yaml"
+    if command -v podman &>/dev/null && podman compose version &>/dev/null 2>&1; then
+        podman compose $COMPOSE_OPTS logs {{ args }}
+    elif command -v docker &>/dev/null && docker compose version &>/dev/null 2>&1; then
+        docker compose $COMPOSE_OPTS logs {{ args }}
+    else
+        echo "[ERROR] Need podman or docker with compose"
+        exit 1
+    fi
+
+# Open bash in running devcontainer
+[group('devcontainer')]
+shell:
+    #!/usr/bin/env bash
+    if [[ -f "/.dockerenv" ]] || [[ -n "${container:-}" ]]; then
+        echo "[ERROR] Run this from the host: just shell"
+        exit 1
+    fi
+    cd .devcontainer || exit 1
+    COMPOSE_OPTS="-f docker-compose.yml -f docker-compose.project.yaml"
+    [ -f docker-compose.local.yaml ] && COMPOSE_OPTS="$COMPOSE_OPTS -f docker-compose.local.yaml"
+    if command -v podman &>/dev/null && podman compose version &>/dev/null 2>&1; then
+        podman compose $COMPOSE_OPTS exec devcontainer bash
+    elif command -v docker &>/dev/null && docker compose version &>/dev/null 2>&1; then
+        docker compose $COMPOSE_OPTS exec devcontainer bash
+    else
+        echo "[ERROR] Need podman or docker with compose"
+        exit 1
+    fi
+
+# Restart service(s)
+[group('devcontainer')]
+restart *args:
+    #!/usr/bin/env bash
+    if [[ -f "/.dockerenv" ]] || [[ -n "${container:-}" ]]; then
+        echo "[ERROR] Run this from the host: just restart"
+        exit 1
+    fi
+    cd .devcontainer || exit 1
+    COMPOSE_OPTS="-f docker-compose.yml -f docker-compose.project.yaml"
+    [ -f docker-compose.local.yaml ] && COMPOSE_OPTS="$COMPOSE_OPTS -f docker-compose.local.yaml"
+    if command -v podman &>/dev/null && podman compose version &>/dev/null 2>&1; then
+        podman compose $COMPOSE_OPTS restart {{ args }}
+    elif command -v docker &>/dev/null && docker compose version &>/dev/null 2>&1; then
+        docker compose $COMPOSE_OPTS restart {{ args }}
+    else
+        echo "[ERROR] Need podman or docker with compose"
+        exit 1
+    fi
+
+# Open Cursor/VS Code attached to the running container
+[group('devcontainer')]
+open:
+    #!/usr/bin/env bash
+    if [[ -f "/.dockerenv" ]] || [[ -n "${container:-}" ]]; then
+        echo "[ERROR] Run this from the host: just open"
+        exit 1
+    fi
+    if command -v cursor &>/dev/null; then
+        cursor .
+    elif command -v code &>/dev/null; then
+        code .
+    else
+        echo "[ERROR] Neither cursor nor code found. Install Cursor or VS Code."
+        exit 1
+    fi
+
+# -------------------------------------------------------------------------------
+# SIDECAR
+# -------------------------------------------------------------------------------
+
+# List available sidecar containers
+[group('sidecar')]
+sidecars:
+    #!/usr/bin/env bash
+    echo "Available sidecar containers:"
+    echo ""
+    # Get running sidecars (exclude devcontainer itself)
+    # Construct podman format string using printf to avoid just interpolation
+    FORMAT=$(printf '\x7b\x7b.Names\x7d\x7d')
+    RUNNING=$(podman ps --format "$FORMAT" 2>/dev/null | grep -v "^devcontainer$" || true)
+    if [ -n "$RUNNING" ]; then
```

The sidecar recipes call `podman` unconditionally (`podman ps/start/exec`), but earlier devcontainer recipes explicitly support both Podman and Docker. On hosts that only have Docker available, these sidecar commands will fail even though `just up/down` would work. Consider reusing the same runtime detection pattern here (podman-first but docker fallback) and using the selected runtime for ps/start/exec.

Conversation:

- **[@c-vigo](https://github.com/c-vigo)** on March 9, 2026 at 12:43 PM — [link](https://github.com/vig-os/devkit-smoke-test/pull/21#discussion_r2905198753)

  Addressed in 1af5337. Sidecar recipes now detect runtime (`podman` first, fallback to `docker`) and use the selected runtime consistently for `ps`, `start`, and `exec`.


---
---

## Commits

### Commit 1: [15b7d50](https://github.com/vig-os/devkit-smoke-test/commit/15b7d503c390127ac5cfc4e16da52dae96a699f1) by [c-vigo](https://github.com/c-vigo) on March 9, 2026 at 10:36 AM
chore: update devcontainer image and template, 1636 files modified

### Commit 2: [9bccd87](https://github.com/vig-os/devkit-smoke-test/commit/9bccd87329a0bcb66e493c6c2d175be07ee33ba4) by [c-vigo](https://github.com/c-vigo) on March 9, 2026 at 10:46 AM
docs: update CHANGELOG, 3 files modified (CHANGELOG.md)

### Commit 3: [ac31d8a](https://github.com/vig-os/devkit-smoke-test/commit/ac31d8a540cd8d11b633ab06fdeec482671298e7) by [c-vigo](https://github.com/c-vigo) on March 9, 2026 at 12:42 PM
chore: update devcontainer image and template, 81 files modified (.devcontainer/justfile.devc, .devcontainer/justfile.gh, .devcontainer/justfile.worktree, .github/label-taxonomy.toml, .vscode/settings.json, CHANGELOG.md, justfile.project, uv.lock)
